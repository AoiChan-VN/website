/
├── index.html              # Entry point (SPA logic)
├── robots.txt              # Cấu hình bot tìm kiếm
├── .htaccess               # Bảo mật cơ bản (tùy biến cho server hỗ trợ)
├── security.txt            # Thông tin liên hệ bảo mật chuẩn RFC 9116
├── /assets/                # Tài nguyên tĩnh
│   ├── images/             # Logo, Banner, Thumbnails
│   └── icons/              # Favicon, SVG icons
├── /db/                    # Database nội bộ (JSON)
│   ├── plugins.json        # Dữ liệu Plugins
│   ├── resources.json      # Dữ liệu Resource Packs
│   └── youtube.json        # Dữ liệu Video/Kênh
├── /scripts/               # Logic điều khiển
│   ├── app.js              # SPA Router & Core Logic
│   ├── data-loader.js      # Xử lý fetch dữ liệu từ /db/
│   └── security-core.js    # Chống XSS, bảo vệ nội dung
└── /styles/                # CSS phân tách (Import)
    ├── main.css            # File tổng hợp @import
    ├── variables.css       # Màu sắc, font (Global)
    ├── layout.css          # Grid, Flexbox, Responsive
    ├── components.css      # UI (Card, Button, Nav)
    └── animations.css      # Hiệu ứng mượt mà
